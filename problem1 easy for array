#include <iostream>
using namespace std;
int
main ()
{
  int arr[200], n;
  cin >> n;
  for (int i = 0; i < n; i++)
	{
	  cin >> arr[i];
	}
  for (int i = 0; i < n; i++)
	{
	  if (arr[i + 1] <= arr[i])
		cout << "NO";
	  // break;
	  else
		cout << "YES";
	  break;
	}
  return 0;
}
